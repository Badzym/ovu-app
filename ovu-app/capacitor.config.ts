import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.app',
  appName: 'ovu-app',
  webDir: 'dist/ovu-app/browser'
};

export default config;
